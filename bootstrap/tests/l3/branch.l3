i32 sign(i32 n) {
  if (n < 0) { return 0 - 1; } else { if (n > 0) { return 1; } else { return 0; } }
}

i32 classify(i32 n) {
  i32 r = 0;
  if (n % 2 == 0) { r = 100; } else { r = 200; }
  return r + sign(n);
}

i32 main() { return classify(4) * 1000 + classify(0 - 3); }
