[package]
name = "wax"
version = "0.1.0"
edition = "2021"

# No dependencies, deliberately. An assembler whose job is to be auditable by reading should not
# begin by asking the reader to audit a dependency tree.

[[bin]]
name = "wax"
path = "src/main.rs"

[lib]
name = "wax"
path = "src/lib.rs"
