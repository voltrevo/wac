// expect: refused
// only:   wacc
// why: an attribute answers to the field's declared type, not to string
// from: design/lang/0005
import { Attr, Node } from core;

struct Card {
  i32 count;

  Node render(const this, Node[] kids) { return <p>{Node.Text("")}</p>; }
}

export i32 run() {
  Node n = <Card count="three"/>;
  return 0;
}
