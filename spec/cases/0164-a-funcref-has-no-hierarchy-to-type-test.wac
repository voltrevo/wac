// expect: refused
// why: operators.md:84 — is with a type wants a reference with a hierarchy, so a fn[] is refused
// from: issues/lang/0125
struct P { i32 v; }
export bool f(fn[void()] g) { return g is P; }
