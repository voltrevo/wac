// expect: refused
// why: the call is inside a construction, and it is still a call
// from: specSingle's §wac-modconst-notconst cluster
struct P { i32 x; }
i32 f() { return 1; }
const P A = P(f());
export i32 g() { return A.x; }
