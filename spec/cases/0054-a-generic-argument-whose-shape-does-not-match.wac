// expect: refused
// why: the parameter is Box<T> and the argument is a P — there is no T that makes those the same
// from: specSingle's §wac-generic-fn cluster
struct Box<T> { T v; }
struct P { i32 v; }
T unbox<T>(Box<T> b) { return b.v; }
export i32 f() { P p = P(1); return unbox(p); }
