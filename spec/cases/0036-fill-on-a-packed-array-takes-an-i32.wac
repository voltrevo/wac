// expect: answers f = 7
// why: a packed element widens to i32 on the way out, so an i32 is what fills one
// from: packages/zstd/src/block.wac, which an over-strict element check refused
export i32 f() {
  u8[] bytes = u8[4]();
  i32 v = 7;
  bytes.fill(v, 0, 4);
  return bytes[3];
}
