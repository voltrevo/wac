// expect: refused
// why: a type argument is inferred from what the argument says, and a call through a funcref says
// why: nothing a reader can name — `T` is not evident here
T id<T>(T a) { return a; }
i32 inc(i32 a) { return a + 1; }
export i32 f() { fn[i32(i32)] g = inc; return id(g(1)); }
