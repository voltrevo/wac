// expect: refused
// why: `if (s is Circle)` narrows exactly as a match arm does, and the same const rule follows
enum Shape { Point, Circle(f64 radius) }
export f64 f() { Shape s = Shape.Circle(1.0); if (s is Circle) { s = Shape.Point; } return 0.0; }
