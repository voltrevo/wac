// expect: refused
// why: an element is a write through the reference, exactly as a field is
// from: specSingle's §wac-const-deep cluster
export i32 bad() {
  const i32[] a = i32[](1, 2, 3);
  a[0] = 99;
  return a[0];
}
