# Open issues

Newest first. See `README.md` for what belongs here and what does not, and `closed/` for the
record of what has been fixed and why.

| # | summary | kind | symptom |
|---|---|---|---|
| [0306b](closed/0306b-socks-traps-on-the-v8-host-when-a-client-connects-and-says-nothing.md) | A client that connects and says nothing killed the proxy: `wac: packages/tor/src/socks.wac trapped`, with no `$trap$message`. **Two of our own defects, one hiding the other.** `run_main` called `tc.reset()` one statement before the branch that reports a trap, discarding V8's exception — so every engine-level trap printed a bare `trapped` and the real reason was in a local nobody read. It said `recv on something that is not a connected socket or a child`: a pump parked in `recv` while another pump's continuation closed that socket, and this host **threw** where wasmtime and Deno answer `Read.Failed`. The rule was already written one branch up for `PARENT_FS_HANDLE` — *an absent parent is an answer, not a fault* (0148) — and never generalised. 5 of 5 before, 0 of 5 after, and the regression case is now committed. Nothing in `socks.wac` was wrong; ten hypotheses went to the async lowering because I read an empty trap message as evidence about the fault rather than about the reporter | bug | closed |
| [0307b](closed/0307b-the-wasmtime-host-still-drops-an-unclaimed-datagram-0207s-fix-landed-in-one-host.md) | `recv` hands reading to a thread, and `Stream::read` blocks and then **drains** — so a caller who gives up does not stop it: the bytes arrive, are taken out of the queue, and were handed to a ticket nobody held and dropped. The next `recv` began after a hole. **Both** Rust hosts, not one: the v8 host had `0207`'s machinery and spent it on datagrams only. Fixed with `Stream::unread`, which puts them back at the front and notifies — a side table was the first fix and it passed once, because two readers park on the same queue and the abandoned one filed the bytes where the retry would never look. The title says datagram and is wrong; the number is what refers to it | bug | closed |
| [0308b](closed/0308b-dropping-a-ticket-throws-away-an-answer-that-had-already-arrived.md) | The other order from `0307b`: the bytes arrive first and are filed under a still-live ticket, and *then* the caller gives up — so `drop` finds an answer in hand and bins it. **0 of 10 kept on both hosts, against a 6-of-6 control.** I closed `0307b` calling this the narrowest path of all and leaving it as a comment; it is neither narrow nor unmeasured. Reached by the bounded-read idiom `platform.wac` documents, which `packages/tor` uses in three places. The fix needs a ticket's handle, which an id does not carry — the v8 host keeps that map for datagrams already | bug | closed |
| [0309b](open/0309b-sshd-serves-one-connection-at-a-time.md) | `while (true) { accept; serve(...); closeSocket; }`, and every read inside `serve` blocks — so a second client sits in the kernel backlog until the first hangs up, which reads as a slow server rather than a busy one. `relayd` and `socks` both shed this shape on 2026-08-30 and the async pump machinery is proven. Filed rather than done because it is a **decision**: concurrent sessions raise what two clients editing one `Fs` should see, and `o.image`'s save-on-exit is written for a single writer — picking silently is how the second client loses its work | missing feature | one session at a time |
| [0310b](closed/0310b-accept-orphans-the-connection-it-took-when-nobody-collects-it.md) | Which *other* capabilities consume something irreplaceable and hand it to a ticket that may be gone? Four, and two were broken. `readStdin` read to EOF for a dead ticket and dropped the lot — **1 of 8 kept**, now 8 of 8. `accept` takes a connection, puts it in the handle table and drops the answer — **0 of 8 → 8 of 8**, by completing a *live* ticket rather than parking the socket — parking measured 0 of 8 unchanged, because the retry is already blocked in `accept()` and cannot be handed a connection. Both sites carried the same false "nothing was consumed" comment a script wrote | bug | closed |
| [0311b](open/0311b-the-javascript-hosts-release-an-answer-that-had-already-arrived.md) | `call.ts`'s `cancel` does `if (status === ST_READY) { release(b, t.slot); return; }` — an answer already in the slot is given back unread, which for a `recv` is bytes taken off a socket once. That is `0308b` on Deno, Node and the browser, and the other branch is `0307b`. Reached from `Pending.cancel` via `drop`, so the path is real. **Not measured** — the Rust hosts were fixed against numbers and this has none, which is exactly how `0307b` was filed wrong; the first job is a number, through `appRun` | bug | four hosts, two checked |
| [0305b](closed/0305b-a-second-stream-to-a-target-that-already-served-one-gets-no-answer.md) | The exit opens the stream, forwards the 68-byte `GET`, and the target answers with nothing — `0 bytes out` where the same target on the same circuit had written 2,561 an exchange earlier. Client B, on a fresh target and a fresh circuit, worked. Not `relayd`'s one-stream-per-circuit limit (that refuses with RESOURCELIMIT and says so, and this stream opened), not `dird` being single-shot (`dird_test` makes three requests on three connections every gate), not `0304b` (fixed first). What distinguishes A is a target that had already served a request **and** a circuit that had already carried a stream; which of the two matters is one run each. Filed rather than chased because the two-client case works with fresh targets, so the `socks` rewrite is not blocked — but a symptom that lives only in a code comment is one nobody runs down | bug | a request reaches the exit and the target answers with no bytes at all |
| [0304b](closed/0304b-a-close-does-not-reach-the-peer-while-a-read-is-outstanding.md) | `closeSocket` is delivered when the socket is idle and swallowed when a `recv` is pending. Three arms differing only in what is outstanding at the close: nothing → the client sees `End`; a **live** read → nothing; a **cancelled** read → nothing. So a client reading to end-of-stream hangs, and a server leaks the connection. Filed first as "`socks` does not close a client's socket" — `socks.wac` cancels before closing and `dird.wac`, whose peers do see `End`, does not, which made cancelling the obvious suspect and it is wrong. The first probe could not have told: it issued the read in both arms and called the uncancelled one the control, so the control failed with the case and proved nothing. Unseen until now because a close only matters to whoever reads, and `socks` is the first program here anything ever spoke to and then read to end-of-stream | bug | a server's close never reaches a client that is reading, whenever a read is parked on the connection |
| [0303b](open/0303b-socks-is-the-last-hand-rolled-multiplexer-and-has-no-test-to-change-it-under.md) | `socks.wac` rebuilds a `waitAny` list by hand every pass — the guard link, the listener and one `recv` per client — with an `owner` array beside it whose own comment says "an arithmetic slip here routes a stranger's bytes". It is the last hand-rolled multiplexer in `packages/tor`, `dird` and `relayd` having both gone async, and it is **not** blocked by `0300b`: its wait is `waitAny(ids, -1)`, no deadline. What stops the migration is that nothing tests it — `cov_ledger.wac` records it as never exercised and its only end-to-end driver is `test-lane: exclusive`, which the gate does not run — while it binds a port `fuzz_test.wac` calls attacker-facing. The fixture is measured and cheap: `relayd` is up in ~1s, serves the directory itself (`-C -K -D`), and writes `socks`'s seed line with `--seedline`; a consensus can be built in-process by `consensusgen.wac`, so no authority and no `dird` are needed, and the test can be the SOCKS5 client over `cli.connect`. Three comments describing an older build sent this the wrong way and all three are now fixed: RSA-is-slow (wrong by two orders of magnitude), `relayd` serves one connection at a time (untrue since it went async), and `relayd` refuses EXTEND2 (untrue since `178f9c0d`, four weeks). So the fixture is three relays rather than one — `socks` is three-hop — and nothing larger is needed | missing test | `socks.wac` has no coverage at all, and no test to change it under |
| [0300c](open/0300c-a-wac-program-can-read-a-symlink-and-cannot-make-one.md) | `Cli` carries `linkStat` — what a *name* is rather than what it leads to — and `Stat.isSymlink` beside it, and nothing that creates a link. So a program can be handed a tree containing links, answer about them correctly, and never make one; `box` has no `ln`, and no wac test can build a fixture that has a link in it. Exactly `chmod`'s shape before `0296c`: the reading half present, the writing half absent, so the vocabulary describes a state nothing in the language can produce. Found twice from opposite directions — `native_hostfs_test` builds its symlink fixture from the harness saying *"there is no `ln` in this system"*, and `box.test.ts`'s wac-mono#25 case (where `tar` walked into a link to a directory and grew the path until it trapped) stays in TypeScript for this reason alone. Not urgent — a gap rather than a defect — but `packages/git`'s checkout is the caller that wants it first, since git's `120000` mode is a symlink | missing feature | not implemented |
| [0302c](open/0302c-a-frame-cannot-be-given-fewer-grants-than-the-program-that-made-it.md) | `childCli` hands the parent's capabilities through unchanged — the frame rewrites the *path*, which is how a child gets a cwd and a redirection, and never touches the **authority**. So a frame models a program's arguments, streams, working directory and redirections: everything about a child except the one thing this system is organised around. Right for the shell, which is the caller it was written for (`0028`: a child is as trusted as the shell), and it means there is no argument that would give a frame *fewer* grants. The cost is countable: five tests in `box.test.ts` each build and spawn a real executable **only** to hold a narrower grant set, and their assertions are exit codes and a sentence. Not visible until everything else in that file had moved in-process. Three things to settle in the issue — what a withheld capability answers, whether the shell keeps today's behaviour (it should), and per-capability versus a set | decision | not implemented |
| [0301c](open/0301c-rev-walks-characters-whatever-the-locale-says-and-wc-does-not.md) | `wc` reads the locale and `rev` does not, and those are the only two applets in `packages/box` whose output depends on where a character begins. `wc` gained `utf8Locale` in `0297c` because GNU splits words differently in `C` and `C.UTF-8`; `rev` walks scalars through `unicode`'s decoder unconditionally, which is right under `C.UTF-8` and is not what the real `rev` does under `LC_ALL=C`. Neither is wrong alone — together they mean `box` has no single answer to "does this program respect `LC_CTYPE`", which a user finds out one applet at a time. Found adding a non-ASCII fixture for the claim that `fold`, `cut` and `tr` are byte-oriented and should be: `rev` cannot join that row, because the capture pins `LC_ALL=C` and the vector would pin an answer we do not want — so the fixture that would have caught the inconsistency is the one that cannot contain it. Three answers in the issue; the code is small either way and picking is the work | decision | inconsistent |
| [0299c](open/0299c-find-ignores-name-and-du-reports-bytes-where-gnu-reports-blocks.md) | `find . -name m1.txt` prints the **entire walk** — the filter is accepted and ignored, so matching nothing and matching everything are the same command. `lib/flags.wac` exists to refuse a flag an applet has not implemented, and `-name` escapes it by taking a *value*, which the shared parser reads as an operand; `flagsChecked` does not list `find`. Never compared before, because the Deno sweep asked `find` only through a whole-tree walk, where a filter that ignores its argument and one that has no argument look identical. **Filed with two companions that turned out to be mine to get wrong**, and corrected the same day: `du` is not reporting the wrong unit, it *is* GNU's `du -sb` and agrees with it exactly, which the sweep compares against one line from where I was reading; and `find`'s sorted order is normalised by that comparison sorting both sides, not a divergence nobody noticed | bug | wrong answer — a filter that filters nothing |
| [0298c](open/0298c-the-ladder-ignores-diagnostics-and-writes-an-exportless-module.md) | A file with a plain type error — `st.mode` where `Stat` has no `mode` — is reported by `wac build` with a line and a column and exit 1, and built by the **ladder** with *"wacc built …: 171344 bytes"* and exit 0. The module it writes has `"exports": []`. `bootstrap.sh` builds the `wac` command through that route, so a type error anywhere in its graph yields a seed exporting nothing, and the binary around it answers every subcommand with *"exports no `task`. It exports:"* — naming neither file nor fault. **The recovery tooling goes with it**: the stale-embedding guard says to run `wac task gen:core`, which is one of the commands that no longer runs. Only `bootstrap.sh`'s own fixpoint check catches it, and only because the thing being built is the command; any other program built through the ladder has no such guard. Found while adding `0296c` — the signal was the artefact coming out 15 KB *smaller* than the last good one | bug | no error — a program with a type error builds and exports nothing |
| [0297c](closed/0297c-box-wc-ignores-the-locale-and-is-wrong-under-lc-all-c.md) | `printf 'a\xc2\xa0b\n'` is **2** words to `wc -w`, **1** under `LC_ALL=C` — and 2 to `box wc -w` in either, because box's `wc` never reads the environment: `isWordSeparator(cp)` is a fixed table applied unconditionally, which is the UTF-8 answer everywhere. `LC_ALL=C` is what this repository compares in and box says so in three places — `ls.wac`, `args.wac`, `trset.wac` — so its applets have silently picked two different locales, C for `ls`/`tr`/the argument parser and UTF-8 for `wc`. Neither half of the suite can see it: the `wc` vector cases are pure ASCII where the locales agree, and the one test with non-ASCII deliberately does not pin, for a reason that was right about an earlier bug. Fix is `wc` reading `LC_CTYPE`/`LC_ALL`; `Cli.env` already exists. Then the capture pinning stops blocking that case from lifting into vectors, which is what `0193` wants. Audit beyond `wc` — `sort` collation, `tr` classes, `grep` — is the follow-up. **Fixed 2026-08-30** — `utf8Locale` reads the POSIX precedence and `wc` walks bytes or scalars accordingly; the differential now grants `env`, because a box without it falls back to C and was being compared against a `wc` that had the ambient environment | bug | done |
| [0296c](open/0296c-put-all-four-hosts-on-path-as-wac-host.md) | Developing here, all four hosts on PATH as `wac-v8`, `wac-wasmtime`, `wac-nodejs`, `wac-deno`, with `wac` the v8 one, so a test that wants a host names it and shells out. **Measured to work already**: a `wac app` artefact built by the v8-hosted `wac` runs unchanged under `wac-deno app-run`, which had never been exercised — so *"a program for host X"* was never a build variant, it is one artefact and a choice of runner. That removes the last argument for `build.ts`'s `deno` and `node` targets, which `design/system/0009` kept for five host-comparison tests on the grounds that *"those targets are what the tests compare"* — they want a runner. It also turns `0295c` into a two-line differential, and that bug is exactly what the absence costs: two tests about fault agreement pass while both Rust hosts are missing fault 6, because one compares TypeScript against wac and the other compares the Rust hosts against each other. Open: which hosts are built by default (two need cargo), which directory owns them, and `wac` as a symlink | missing feature | not implemented |
| [0295c](open/0295c-the-rust-hosts-report-an-unnameable-path-as-absent.md) | A file whose name is not valid UTF-8 — ordinary on Linux, `cat` reads it from bash — is reported by both Rust hosts as **not there**. `ls` mangles the byte to U+FFFD on every host, which is not the bug; what differs is the answer about the mangled path. Deno says *cannot be named on this host*, native v8 says *No such file or directory*, from the same `box` source. That is the exact answer `0065` was written to eliminate, because it reads as the caller getting the name wrong, and because `test -e` then answers no about a file that is there — live on the **default** host, the one `wac app` runs on. `FAULT_NOT_REPRESENTABLE` is 6 and neither Rust host has it: both jump `FAULT_OTHER = 5` to `FAULT_NOT_GRANTED = 7`. **Two tests exist for this and both pass**, because the numbering exists four times and they cover different pairs — `faults_agree.test.ts` compares TypeScript against wac (both have 6) and `hostfaults_test.wac` compares the two Rust hosts against each other (both missing it equally). A gap present in both members of a pair is invisible to a test that asks only whether the pair matches | bug | wrong answer — a file that is there is reported as not there |
| [0294c](open/0294c-sshd-serves-one-client-at-a-time-and-the-fs-depends-on-it.md) | `sshd`'s accept loop calls `serve` — a whole SSH session — blocking, so a second client cannot connect until the first disconnects. With `async`/`await` landed that is a three-line change and the shape's test already passes, **but the blocking is load-bearing and the file says so**: every session shares one in-memory `Fs`, and the comment above the loop records that "one writer" is true by construction rather than by a lock, and that the concurrency question `design/system/0001` leaves open "stops being closed the day this serves two at once". So the port silently reopens it — nothing would fail, two sessions would simply interleave writes. Two answers are needed first and neither belongs to the lowering: what one `Fs` under two sessions means (per-session, copy-on-write, or a lock), and when the image is saved, since `save(…)` runs after a session ends and that stops naming a quiet moment. `packages/tor/src/dird.wac` is the same shape with its state threaded in the source rather than in a comment — `docs = answer(core, cli, conn, docs)` — and is the stronger case of the two, since its own comment records that a client which never finishes a request line holds the loop for everyone. Found by the survey for where `async` is a clear win, which also measured that `.then(` has **no** adoption outside the embedded platform source — 3080 `.wait()` against 13 `.then(`, all 13 in `coretext.wac`/`emit.wac` — so the opportunity is code that never became concurrent, not callback code to tidy | design decision | a second client cannot connect until the first disconnects |
| [0293c](open/0293c-the-bodies-of-core-and-std-are-never-type-checked.md) | `wac check core/hash.wac` answers *"1 file(s), no diagnostics"* about a file containing `i32 b = "nope";` — the same bytes copied to any other path report the error. `std/platform.wac` is the same, as the entry and through an importer. **Not `0291b`**: it reproduces with a freshly regenerated embedding, so both copies carry the error and neither is checked; a body error changes no export list, so code 210 cannot fire. Nor is it "only the entry is checked" — an ordinary imported file's body *is* checked. So the nine most-imported files in the tree have no type-checking at all, and the only thing that notices is the engine refusing the finished module at the end of a full `bootstrap.sh`, with no line and a message blaming the compiler. `issues/lang/0292c` is what it cost: a duplicate member — a diagnostic wacc fires for plain, generic, exported, separated and differently-typed members alike — read as a compiler bug about generic host-bound structs, and narrowed by three full bootstraps over a variable that was never the one | bug | wrong answer — a file with a plain type error is reported as clean |
| [0292b](open/0292b-base64-and-base32-accept-an-unparseable-w-and-use-the-default-width.md) | `box base64 -w banana` encodes and exits 0 where GNU says *invalid wrap size* and exits 1; `base32` is identical. `-w` itself is correct — `-w 20` wraps at twenty, byte for byte with GNU — so this is the *argument* being unvalidated, and `hasNum` being false is indistinguishable from the flag not being given, so the width falls back to 76 and any input under 76 encoded columns looks right. **Scope measured, not inferred:** eleven applets read `a.hasNum ? a.num : default` and only these two have the hole — `fold`, `head`, `tail`, `split`, `strings` and `shuf` all refuse. Found by `flags:ignored`, which also falsifies `packages/box/README.md`'s *0 ignored against 7 refused*: the same 375 flags and 43 applets now answer 2 against 4 | bug | no error; encodes as though the flag were absent |
| [0291b](closed/0291b-editing-std-or-core-does-nothing-until-gen-core-runs-and-nothing-says-so.md) | `std/platform.wac` and `core/*.wac` are carried **inside** the compiler as `packages/wacc/src/coretext.wac`, so an import of them never touches the disk — and `./bootstrap.sh` does not run `wac task gen:core`. Add an export to `std/platform.wac`, rebuild, import it: *"that file does not export this name"*, pointing at the import, about a file that plainly does export it. Verified with a one-line probe. Two separable faults: the diagnostic states the opposite of the truth where the resolver knows enough to name the stale embedding, and `gencore_test.wac`'s check runs in the suite — after the build it would have explained. Nine files are in scope, and both READMEs said only "do not edit the embedding by hand", which reads as though editing the source is safe. **Both fixed 2026-08-30**: `bootstrap.sh` refuses a stale embedding before the ladder, and a built-in import that names something the compiler's copy lacks gets its own code — *"the compiler's copy of that built-in does not export this name"*, with `wac task gen:core` in the help | bug | wrong answer — a file that exports a name is said not to |
| [0290b](closed/0290b-a-wac-program-cannot-run-a-host-binary-in-a-chosen-directory.md) | `Cli.execWith` is the only capability that runs a **host binary** and it takes no working directory, while `spawn`, `spawnSelf` and `pushChild` all take one and none of them can run a host binary — and `Cli.cwd` is deliberately read-only, so a program cannot set its own either. 24 mentions of `cwd:` across 14 files in `tools/` and `harness/` need it: all five `corpus:*` tools, `mutate`, `ignoredFlags`, and `buildApp`/`appRun`/`appRunMany`/`bounded`, which is most of `0289b` steps 1, 4 and 5. The `sh -c 'cd DIR && …'` workaround is wrong for the tool that found this, because the payload is a shell corpus full of quotes and semicolons and `args` is documented as an argument vector precisely so those arrive whole. **`spawn` had this same omission and it was fixed** — its `cwd` doc says `pushChild` took a directory from the start "for exactly this reason and `spawn` did not". ~~Blocked by `issues/lang/0291c`~~ — **not blocked**: that closed on 2026-08-30 as not reproducing, its recipe having edited `std/platform.wac` without regenerating the compiler's embedded copy (`0291b`). **Done 2026-08-30**: `Cli.execWithIn` is the capability and `execWith`/`exec` are methods over it, so no call site moved; opcode 59 rather than a wider 58, because a stale host must fail by name rather than read the directory as the head of standard input. Both native hosts run the same built program and answer `/tmp` for a directory and their own for none | missing feature | not implemented |
| [0289b](open/0289b-tools-and-harness-are-typescript-and-nothing-owns-moving-them.md) | `tools/` and `harness/` are **15,437 lines of TypeScript in 82 files** and no issue owns moving them — `0161` covers `packages/**/*.test.ts` and says in as many words that these two directories are "a different project". The order matters and is not obvious: `coverageOrder.ts` looked like the ideal first port and was a *library* that had to move with its importer; `suiteGate.ts` cannot be deleted until the eight tools that announce themselves have moved. Three of `0161`'s recorded blockers no longer hold — `docSignatures` and `designClaims` were already ported, and `seedFresh` was kept for a lane argument that does not apply to its call sites, since `push.sh` invokes it directly; it is `tools/wac/seedfresh_test.wac` as of 2026-08-30. `tools/mutate/` is 2,941 of those lines and `ls tools/*.ts` does not show it. Recorded on 2026-08-30 as **the unblocked remainder is nothing**, and false within the day: `0291c` did not reproduce, `0290b` landed, and `corpus:through` and `flags:ignored` are ported. Seven of the nine remain, plus the carve-outs where Deno is the oracle or the subject — including both fuzzers, which I first recorded as available and was wrong about | missing feature | not implemented |
| [0288b](open/0288b-packages-sh-is-measured-and-not-ratcheted.md) | `packages/sh` has a coverage task and no ledger, so its 1,036 uncovered points hold no floor and the gate's summary counts it without naming it | bug | no error |
| [0287b](open/0287b-the-build-cache-is-off-on-every-host-but-one.md) | only `native/v8` sets `WAC_COMPILER_ID`, so `cacheKey` refuses to key a build on any other host and the spec says it caches | bug | no error |
| [0286b](open/0286b-a-one-package-test-run-spends-the-twenty-minute-push-budget.md) | `wac task test <package>` stamps the cooldown, so 1.3s of work refuses the next push for twenty minutes | bug | no error |
| [0285c](closed/0285c-a-spawned-applet-reads-its-parents-leftover-input-instead-of-the-file-it-opened.md) | On the V8 host a spawned applet that has called `openInput` reads **its parent's queue** instead of the file: `printf 'one\nA\nB\n' \| wac sh -c 'read v; cat MERGE.md'` prints `A` and `B`. Usually the parent has nothing left, so `wc -c f` answers a plausible **`0`** and `sha256sum` gives the hash of the empty string. `Cap::ReadStdin` and `Cap::ReadChunk` both check the parent's queue *before* the `openInput` redirect, so a redirect wins over the frame's queue and loses to the parent's — the frame half of that rule was fixed and the parent half was left, in the same function. wasmtime and Deno are right. Scope is every file-reading applet: 37 files import the `input.wac` helper | bug | file contents replaced by the parent's input, exit 0 |
| [0284c](open/0284c-the-hosts-word-a-refusal-differently-and-only-one-says-which-capability.md) | A program built without `--allow-read` is told `filesystem read not granted to this application` by the JavaScript hosts and `Not granted to this application` by both Rust ones — which names no capability, and is the same string for all five. A program can be built without several at once, so the shorter sentence leaves the reader to work out which, with nothing in the message, the file name or the status to say. Surfaced by `world_test.wac`, whose case for a withheld capability asserts the informative wording | diagnostic | one refusal, two sentences |
| [0283b](open/0283b-taking-the-gate-lock-starts-the-cooldown-before-anything-runs.md) | `tools/push.sh` takes the gate lock before it pulls, and taking it writes the 20-minute cooldown — so a conflicting pull locks the agent out having tested nothing | bug | no error — twenty minutes lost per occurrence |
| [0282c](closed/0282c-a-relayed-programs-stderr-does-not-interleave-with-its-stdout.md) | `echo one; nope; echo two` through a `wac app` shell writes `one / two / sh: nope: command not found` where bash — and the same shell through `wac run` — put the diagnostic between the two. Each stream carries the right bytes; their order relative to each other is lost, which is only observable when both go to one place. `relay` waits on both with `waitAny` and takes standard output first when both are ready, and a child writes into **two queues** with nothing recording which write came first — so the order was never carried and reordering the branches would only change which stream wins. Needs one channel with a tag per chunk, or a sequence number. Blocks `packages/sh/test/differential.test.ts` in `design/system/0009` | bug | right bytes, wrong order between streams |
| [0279c](open/0279c-the-conformance-ledger-credits-fifteen-opcodes-to-tests-that-skip.md) | `conformance_test.wac` is the ledger of what a second host must answer the same way, and it derives which hosts each cited test *drives* rather than trusting a written list. It does not know a cited test can **skip**: three files needing the wasmtime host — which `CLAUDE.md` says is not built by default — carry 30 citations between them, and for **15 opcodes that is the only comparison there is**, including `OPEN_OUTPUT` and `SPAWN_SELF`. Those are `0277c` and `0276c`, both found by hand the same day. `CLOSE_SOCKET` shows the hole is per *host* rather than per opcode: cited twice so it counts as doubly covered, but the second citation compares Deno against Node and the bug was in v8. The push gate is such a checkout — its own output says `cargo did not build native, so nothing was compared`, five times a run | missing feature | a coverage number counting comparisons that did not happen |
| [0274c](open/0274c-wac-app-coverage-instrumented-a-module-nothing-could-read.md) | `wac app --coverage` was accepted, instrumented the module, and wrote no `.cov` — the table naming the counters is `build`'s arm, and `covdump` refuses an app anyway because of the two shell lines on the front. So the entire effect of the flag was the instrumentation overhead it silently paid for. Two ordinary decisions that are only wrong together: `unknownFlag` is **one list for every command**, so `app` advertised five flags and took nine; and `covered` is decided in the shared compile *above* the dispatch, which is right for `build`, `test` and `run`. **The flag is refused as of 2026-08-29** and the test asserts on the message, not the status. What stays open is coverage for an app at all — `design/system/0009` moves ~47 `buildApp` calls onto `wac app`, and `build.ts` instruments them deliberately, so the attribution goes unless something answers what writes the table, how `covdump` seeks past the preamble, and when a running app dumps | missing feature | a flag that did half its job silently |
| [0275b](open/0275b-where-the-gates-twelve-minutes-go.md) | the gate's twelve minutes, measured: 1283s of work at 4 workers, a 357s floor, and which file each chunk is — a router to the issues that own each piece | performance | no error — the gate is what everyone waits for |
| [0267c](open/0267c-the-instrument-for-priority-3-cannot-see-32-of-38-packages.md) | `runOnWacc.ts` prints the README's priority-3 figure — *34 of 34 packages pass their own suite on wacc-emitted code* — by shelling out to `deno test`, which finds `*.test.ts` and nothing else. **32 of 38 packages now have only `*_test.wac`**, so the tool reports `FAIL … 0 passed` for each of them and can measure 6. The figure was recorded 2026-08-12 and `0161` has been moving packages off Deno since. The decision is what it should measure now: a `*_test.wac` is compiled by wacc by construction, so "passes on wacc-emitted code" cannot fail for it — the sweep is not only blind today, it goes vacuous as the migration finishes. Measured: **6 of 38 (233 tests)** against a README figure of 34 of 34 (1,663), and the run also turned up a missing `--unstable-net` — fixed — that had been reporting `platform` as a wrong answer on a suite that is 105 of 105 | decision | a green figure measured over a seventh of what it names |
| [0266c](open/0266c-the-suite-gates-memory-floor-refused-every-push-for-a-day.md) | thirty-five `tools/push.sh` attempts in one day, every one refused before the suite started, always for memory: available read 4930–5520 MB against a floor of 5500, and it crossed once. Three resident agents hold 2.28 GB before anything is built, so the floor is missed because agents are *alive* rather than because a suite is running. The floor is derived — 5,158 MB of rise at the default width, from `jobsSweep.sh`'s table in `runTests.wac` — but that sweep runs `deno test` and **not the `wac test` lane**, which is now more than half the suite, so the rise of the suite that actually runs has never been measured. The plausible error is that 5,500 is too *small*, which is what makes forcing meet `0142`'s silent kill and the queue the safe failure | decision | none — 45 finished commits, none pushed |
| [0264c](open/0264c-wac-test-is-implemented-twice-and-the-two-disagree-about-coverage.md) | `native/v8/src/main.rs` had a complete `wac test` — 441 lines plus five helpers — intercepting before the fall-through, so on the native binary the program's `test` never ran. The two disagreed about `--coverage <directory>`: a report per file against one aggregate, both arithmetically right. **The deletion is done as of 2026-08-26** — the fall-through grants the seed read, write, env, net and run outright, which was the blocker. What survives is one defect the probe could not see, because every file in it was a test: measured on a project with a library file, the surviving implementation answers `4 of 6 points (66%)` over the test file alone where the deleted one answered `12 of 14 (85%)` over both, so **the code under test is gone from the table and the denominator**. `sayCoverage` narrows to the walked `*_test.wac`; it should narrow by project root. Either this closes and that refiles, or the title changes to it `testflagrows_test.wac` missed it because it checks flags, not flags *and argument shapes*. **The deletion is blocked, measured 2026-08-25**: routing `test` through the fall-through makes the suite's own oracles fail with *Not granted to this application*, because `run_seed` runs the seed with the grants **baked into its manifest** — read, write, env, net and not run — so a command-line `--allow-run` reaches argv and not capabilities. The 700 lines are a duplicate plus a capability the other route cannot obtain; the seam is `AsChild` | decision | two answers to one command |
| [0261c](open/0261c-env-cannot-say-whether-a-variable-is-unset-or-withheld.md) | `Cli.env` answers `u8[]?`, and now that all four hosts agree on null (`0256c`) that null means both "not set" and "you were not granted the environment" — the conflation `0238c` removed from `Socket` for the reason that a caller acts differently on the two. Nothing is wrong today; the fix is a shape with a fault, and it is measured: forty positional `Cli.of` fields, four hosts and 13 call sites | decision | none — a distinction the type cannot express |
| [0252a](open/0252a-a-frames-output-is-capped-on-two-hosts-and-unbounded-on-the-others.md) | a frame's output is capped at 8 MiB on the JavaScript hosts and grows without limit on the native one, so `Captured.truncated` is always false there — bounded-and-honest against unbounded-and-complete, and `std/platform.wac` documented the cap as the language's | decision | wrong answer |
| [0272b](closed/0272b-wac-test-runs-only-the-first-of-several-paths-and-reports-success.md) | `spec/cli/wac.md` documents `wac test [path…]` as "every `test*` export under each path"; it runs the first and ignores the rest with exit 0 and no warning, so four files asked for answer with one file's count — and with directory-prefixed paths the same walk instead joins them into one unreadable path | bug | wrong answer |
| [0251b](open/0251b-an-sctp-association-ignores-the-peers-advertised-receive-window.md) | `initWindow` — the receive window a peer announces in its INIT — is parsed and never read, so nothing does receiver-side flow control and `send` is bounded only by our own `cwnd`; its twin `initTsn` was unread in the same branch and was a *bug* rather than a gap, fixed the same day | missing feature | not implemented |
| [0250b](closed/0250b-wacc-can-say-what-a-host-reaches-but-not-what-a-file-declares.md) | tools/docSignatures.test.ts resolves a README's backticked `` `foo(…)` `` against every name a declaration introduces — "functions, types, variants, fields, methods" — and `packages/wacc`'s API answers only what crosses the host boundary: `exportSigsFiles` gives exported functions, `bindTypesFiles` host-holdable types, and nothing gives methods, enum variants or unexported functions, so porting that check onto our own parser would silently narrow the half that found both bugs it was written for | missing feature | not implemented |
| [0241b](open/0241b-a-shared-file-is-measured-once-per-consumer-and-never-as-a-whole.md) | a coverage ledger's prefix is a directory, so a file imported across a package boundary is measured once per importer and never as a whole — `tls/src/handshake.wac` reads 36 of 108 dark from `tls` and 33 from `quic`, and only 15 are dark to both, so "covered by a neighbour" is the one exemption reason nothing can verify | missing feature | wrong answer |
| [0242b](open/0242b-git-inflates-a-remotes-packfile-with-a-decoder-documented-to-trap.md) | `packages/gzip`'s `inflate` traps on a corrupt stream by a stated, fuzz-tested contract — *produces the original bytes or it fails* — and `packages/git` calls it from three network-facing sites, none in a subprocess, while returning `Raw.Damaged` for every other malformation; one flipped byte in a packfile ends the process | bug | trap |
| [0239c](open/0239c-the-compiler-spends-minutes-on-a-file-that-is-not-wac-source.md) | `wac run <a module.wasm>` burns 90s of CPU on 75 KB and writes nothing — the compiler is handed binary as source, and a 36 KB text file finishes at once, so it is superlinear in recovery rather than any one byte; the zero-length output is its own bug | performance | hangs |
| [0234a](open/0234a-a-bare-specifier-means-two-things-and-the-two-hosts-pick-differently.md) | `dep/lib.wac` is a mapping name per the spec and not a relative path, but the Deno walk joins it to the importing file's directory — with `dep/` mapped and a real `src/dep/lib.wac` it compiles the local file and the program answers 99, exit 0, no diagnostic; and the binary resolves a bare specifier no mapping declares as though it were relative, which the spec does not define either | bug | wrong answer |
| [0233c](open/0233c-three-github-design-issues-were-not-captured-anywhere.md) | GitHub 8, 18 and 19 had no record in this tracker: 19's bootstrap rule is already met and checked by `tools/seed.sh`, 18's first question is answered by 0009's D8/D9 — two commits are two modules, so versions can coexist — and 8 is untouched | decision | not implemented |
| [0231c](open/0231c-a-lane-can-be-watched-or-counted-and-not-both.md) | the suite's Deno pass is inherited so it can be watched and is therefore uncountable; its wac lane is buffered so its 2,387 tests can be counted and is therefore unwatchable — one chunk held 66s of output — because `Cli.execWith` has two modes and no incremental read. Not a blocker: a shell redirect plus a bounded `waitAny` and a tail does both today, measured — the capability would make it one process and one read instead of two and a re-read | missing feature | no error |
| [0230c](open/0230c-the-heavy-lanes-declared-costs-are-stale-and-nothing-checks-them.md) | `checked_test.wac` declares `140s, measured` and ran past **1,166s** without finishing; the heavy lane's six files declare 412s and had spent 9m20s of CPU on the first four, and `tools/lane.test.ts` checks that a reason names a number rather than that the number is true | performance | wrong answer |
| [0230a](open/0230a-the-deno-path-is-a-compiler-not-a-wac-command.md) | six of the seven documented subcommands exist as separate Deno entry points and none of them is `wac`; `wac task check` is the *TypeScript* check, and 0229a is what the arrangement already cost | decision | not implemented |
| [0216](open/0216-a-daemon-outlives-the-test-that-started-it-and-a-later-file-pays-for-it.md) | the suite charged `v8host_test.wac` 49.1s of CPU and 49.6s of wall where it costs 1.6s alone; a child's CPU lands on whichever file *reaps* it, and `echod_test.wac` two files earlier leaves Deno peers running — the wall moved too, so something also waits for them | performance | wrong answer |
| [0213](open/0213-the-two-host-tests-moved-to-wac-and-the-javascript-half-became-a-process-again.md) | the three two-host tests are the lane's largest items — 31.0s, 16.3s, 14.6s — because a wac test cannot call `appRunner`, so the JavaScript half is a 133ms process per script where a worker is 1.25ms; `harness/appRunMany.ts` is the same harness behind one `exec` | performance | no error |
| [0213a](open/0213a-the-push-gate-can-starve-a-suite-that-passes-loses-the-race-and-gives-up.md) | four consecutive green suites and nothing pushed — a full run is longer than the interval between other agents' pushes, so losing the race is the expected outcome and the retries get refused | decision | not implemented |
| [0209](open/0209-ed25519-is-five-times-slower-than-p-256-which-is-the-wrong-way-round.md) | the title's ordering is gone — ed25519 and P-256 now cost the same — and all three rows are explained: ed25519 was `ptAdd` inverting per addition, P-256 was byte-wise order arithmetic, and RSA is 2048 rounds of two multiplies and two divisions with nothing hiding. What is left is two changes nobody has made: CRT and a Montgomery multiply | performance | no error |
| [0205](open/0205-fifteen-of-nineteen-coverage-tasks-cannot-fail.md) | fifteen of nineteen coverage drivers end with `report(...)` and cannot fail, while the summary read "19/19 passed" — two hold a floor, two only check their own exemptions | missing feature | no error |
| [0204](open/0204-wac-test-recompiles-every-directory-on-every-run.md) | `wac test` has no build cache, so every run recompiles every test directory — measured at 9% of the lane's biggest chunks, so worth ~8s of wall rather than the 104s it looked like | performance | no error |
| [0203](open/0203-the-gate-fails-one-run-in-six-and-never-on-the-same-test.md) | the gate failed 5 of 28 runs in one day on five different files — two were real breakage arriving through the merge, two were fixed-wait races, one is unexplained | bug | no error |
| [0201](open/0201-a-shell-builtin-shadows-a-box-applet-and-answers-differently.md) | a shell builtin shadows a box applet and refuses in fewer words than the applet does | bug | wrong answer |
| [0198](open/0198-allow-run-also-grants-allow-env-because-exec-inherits-the-whole-environment.md) | `--allow-run` carries `--allow-env`: 37 variables including the proxy. Measured — 21 of 24 exec-using directories need only `PATH`, three name what they need, and the seal test's canary is built on the leak, which settles the fourth parameter | bug | no error |
| [0197](open/0197-a-built-app-costs-107ms-to-start-and-the-same-program-costs-15ms.md) | a built app costs 107 ms to start and the same program through the `wac` binary costs 15 ms | performance | no error |
| [0196](open/0196-the-caret-blink-precondition-still-reds-under-load.md) | the caret-blink test's precondition still reds under load, which is the half `0159` left in front of the guard | bug | wrong answer |
| [0195](open/0195-a-frame-cannot-express-an-open-and-silent-standard-input.md) | a `Frame`'s standard input cannot be open and silent, which is the shape a terminal has | missing feature | a case that cannot be written in process |
| [0193](open/0193-boxs-tests-build-and-spawn-a-world-per-assertion.md) | box's tests build and spawn a world per assertion, and the differentials re-derive their oracle every run | performance | no error |
| [0184](open/0184-a-run-programs-exit-status-cannot-tell-a-trap-from-an-answer-of-one.md) | a run program's exit status cannot tell a trap from an answer of 1 | missing feature | wrong answer |
| [0183](open/0183-mutation-scoring-runs-deno-test-and-twenty-packages-no-longer-have-one.md) | mutation scoring runs `deno test`, and twenty packages no longer have one | bug | no error |
| [0178](closed/0178-the-caret-goes-solid-after-a-click-and-the-test-that-waits-for-it-to-blink-gives-up.md) | the caret stays lit for over a second after a click, and the test waiting for it to go dark gives up | bug | wrong answer |
| [0175](open/0175-a-trap-test-can-observe-nothing-about-the-trap-except-that-it-happened.md) | a `test_traps_*` case can observe nothing about the trap except that it happened | missing feature | not implemented |
| [0173](open/0173-a-wac-test-cannot-say-which-grant-it-needs.md) | a wac test cannot say which grant it needs, so a lane must grant everything | missing feature | wrong answer |
| [0165](open/0165-wac-cannot-run-a-host-program-and-the-best-oracles-are-host-programs.md) | wac cannot run a host program, and the strongest oracles this repository has are host programs | missing feature | not implemented |
| [0164](open/0164-a-trap-case-cannot-take-an-argument-so-a-table-of-lengths-is-a-page-of-exports.md) | a `test_traps_*` case cannot take an argument, so a table of lengths is a page of exports | missing feature | not implemented |
| [0162](open/0162-a-multi-part-answer-is-truncated-under-load.md) | a multi-part answer comes back truncated under load — the improved report says truncated, not crossed | bug | wrong answer |
| [0161](open/0161-moving-the-suite-off-deno-the-order.md) | moving the suite off Deno: the order, and what blocks each step | missing feature | not implemented |
| [0158](open/0158-json-numbers-outside-clingers-window-cost-11x-at-the-boundary.md) | JSON numbers outside Clinger's window cost 11x, at a cliff rather than a slope | performance | wrong answer |
| [0154](open/0154-a-slow-suite-is-starved-at-the-push-because-master-moves-under-it.md) | a slow suite is starved at the push: 45 minutes of green suite, beaten three times, nothing landed | process | no error |
| [0147](open/0147-every-program-pays-for-every-capability-on-cli.md) | every program pays code size for every capability on `Cli`, including the ones it never names | performance | wrong answer |
| [0146](open/0146-a-script-under-site-cannot-use-the-wac-import-map.md) | a script under `site/` cannot use the `wac/` import map, so `wacc-api.js` was never built and the playground fell back silently | build | compile error |
| [0142](open/0142-a-suite-was-killed-with-the-gate-in-place.md) | a suite was killed at the parallel pass with the suite gate in place, and the log says no — reopened: the kill detector reads a cgroup counter that cannot move when the container has no memory limit | bug | no error |
| [0139](open/0139-mutation-testing-cannot-reach-a-low-level-package-in-practice.md) | mutation testing cannot reach a low-level package: nine minutes before the first mutant runs | performance | no error |
| [0138](open/0138-wasmtimes-default-collector-costs-25x-on-escaping-allocation.md) | wasmtime's default collector costs 25x on escaping allocation — fixed by choosing the copying one; a 4x residue is unexplained | performance | no error |
| [0137](open/0137-a-symlink-capability-needs-a-confinement-rule-before-an-implementation.md) | a symlink capability needs a confinement rule before an implementation: its contents are a path, and every rule we have is expressed as a path | missing feature | not implemented |
| [0136](open/0136-the-suite-leaves-its-temp-directories-behind-and-the-disk-filled.md) | the suite leaves temp directories behind — 2,300 of them, and the disk filled | bug | no error |
| [0135](open/0135-a-background-job-runs-the-name-as-an-external-program-so-no-builtin-can-be-backgrounded.md) | a background job runs the name as an external program, so no builtin can be backgrounded | bug | wrong answer |
| [0129](open/0129-every-built-executable-carries-a-floor-that-has-grown-seven-fold.md) | every built executable carries a floor that has grown seven-fold — a `wc` is 347 KiB | performance | no error |
| [0128](open/0128-the-native-half-of-the-two-host-differential-times-out-under-load.md) | the native half of the two-host differential times out under load, and passes alone | bug | wrong answer |
| [0107](open/0107-a-c-tor-fetching-from-our-onion-service-times-out-intermittently.md) | a C tor fetching from our onion service times out intermittently — likely the same fault as 0106 | bug | hangs |
| [0106](open/0106-the-onion-service-test-goes-red-under-load-on-a-shared-machine.md) | the onion-service test goes red under load, and its 30s timeout cannot tell busy from wedged | bug | wrong answer |
| [0097](open/0097-how-much-of-packages-tor-is-ours-rather-than-a-transliteration.md) | how much of `packages/tor` is genuinely ours rather than a transliteration of C tor? | task | no error |
| [0095](open/0095-sha256-is-13x-off-openssl-and-most-of-it-is-not-shape.md) | `sha256` is 13x off OpenSSL, and most of that is not a shape problem | performance | not implemented |
| [0066](open/0066-the-light-client-is-minimal-config-only-and-has-never-seen-a-real-chain.md) | the light client is minimal-config only and has never seen a real chain | missing feature | not implemented |
| [0005](open/0005-mutation-testing-found-54-untested-behaviours.md) | surviving mutants: behaviours nothing checks (crypto finished; tls at 75, five other packages unmeasured) | task | wrong answer |

An empty list is the expected state most of the time — see `README.md`: something you
can fix in a package you are already working in should just be fixed, and a package's
own roadmap lives in its README. This tracker is for what crosses those lines.

## Closed

311 issues, 236 closed.

The count is checked against the directory by `tools/wac/issuecounts_test.wac`, which reads both
trackers. It was `compiler/wacSpec.test.ts` until that file went with the TypeScript compiler on
2026-08-28, taking the only copy of the check with it; the rule is a property of `issues/`, so it
lives beside `issues/` now. It did not read this one until 2026-08-09, and the first thing it found was
thirteen numbers used twice and five issues written in a third header format — neither of
which anything would have noticed, because the rows above happened to be right.
