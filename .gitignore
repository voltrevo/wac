node_modules
dist
*.log
*.tsbuildinfo

# The demos are build output from wac-mono — `tools/syncDemos.ts` writes them and CI runs it on
# every deploy. Committing them meant a megabyte of generated HTML in git that was stale by the
# next day. The rest of `public/` is hand-written and stays.
public/*.html
public/demos.json
